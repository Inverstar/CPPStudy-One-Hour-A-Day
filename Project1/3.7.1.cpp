//关键字表
/*	asm		operator		explicit		export		extern		
	union	virtual			inline			wchar_t	dynamic_cast
	do		volatile		struct			mutable	static_cast	
	template					reinterpret_cast			typename*/

/*	else		new		this	auto		enum	throw	bool	private
	true		break	protected	try	case		public	
	typedef	catch	false	register	typeid	char
	float		class	for	return	const	friend	short
	unsigned		constexpr	goto		signed	using		
	continue	if	sizeof	default	static	void	delete	
	int	long		double	switch	while	namespace	*/

//保留关键字
// and	bitor		not_eq		xor	and_eq
// compl	or		xor_eq		bitand	not	or_eq